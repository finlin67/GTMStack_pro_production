import type { PillarCategoryProps, PillarTopicProps } from './pillarMap'

export type { PillarCategoryProps, PillarTopicProps }

/**
 * Placeholder for Systems & Operations pillar design.
 * Will be replaced with per-pillar TSX when pasted.
 */
export function SystemsOperationsPillarCategory({ children }: PillarCategoryProps) {
  return <>{children}</>
}

export function SystemsOperationsPillarTopic({ children }: PillarTopicProps) {
  return <>{children}</>
}
