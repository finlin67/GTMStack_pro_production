import type { PillarCategoryProps, PillarTopicProps } from './pillarMap'

export type { PillarCategoryProps, PillarTopicProps }

/**
 * Placeholder for Strategy & Insights pillar design.
 * Will be replaced with per-pillar TSX when pasted.
 */
export function StrategyInsightsPillarCategory({ children }: PillarCategoryProps) {
  return <>{children}</>
}

export function StrategyInsightsPillarTopic({ children }: PillarTopicProps) {
  return <>{children}</>
}
