import type { PillarCategoryProps, PillarTopicProps } from './pillarMap'

export type { PillarCategoryProps, PillarTopicProps }

/**
 * Placeholder for Content & Engagement pillar design.
 * Will be replaced with per-pillar TSX when pasted.
 */
export function ContentEngagementPillarCategory({ children }: PillarCategoryProps) {
  return <>{children}</>
}

export function ContentEngagementPillarTopic({ children }: PillarTopicProps) {
  return <>{children}</>
}
