import type { PillarCategoryProps, PillarTopicProps } from './pillarMap'

export type { PillarCategoryProps, PillarTopicProps }

/**
 * Placeholder for Demand & Growth pillar design.
 * Will be replaced with per-pillar TSX when pasted.
 */
export function DemandGrowthPillarCategory({ children }: PillarCategoryProps) {
  return <>{children}</>
}

export function DemandGrowthPillarTopic({ children }: PillarTopicProps) {
  return <>{children}</>
}
